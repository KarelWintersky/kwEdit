tinify(tiny_config['full'], 'area1');

// init
/* $(".tag-button") // button with tag
    .each(function(i, element){
        var $e = $(element);
        var is_present = +(strpos($("input[name='tags']").val(), $e.attr('name')) !== false);
        $(element).attr('data-ispresent', is_present);
})
    .on('click', function(element){
        alert( $(element).attr('name') );
});

$("#action-test").on('click', function(){
    tag_line = $("#tags-input").val();
    $(".tag-button").each(function(i, element){
        $e = $(element);
        $e_name = $e.attr('name');
        alert($e.attr('data-ispresent'));
    });
});*/